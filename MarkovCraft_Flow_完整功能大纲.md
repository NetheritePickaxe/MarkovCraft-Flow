# MarkovCraft Flow — 完整功能大纲（Unity 重构版）

---

## 一、项目概述

### 1.1 定位
MarkovCraft Flow 是基于原版 MarkovCraft（Unity + C#）的深度重构项目。保留 Unity 前端、ECS 实时渲染管线与 CraftSharp-Resource MC 资源解析生态，在可视化编辑、智能生成、多模态输入、导出格式、跨版本兼容与社区生态六个维度进行全面扩展。

本工具是一个**高自由度的通用程序化结构生成器**，类似于 Minecraft 本身的开放沙盒理念——不预设玩法，而是提供强大的底层能力，让用户能够创造任意类型的结构：建筑、地形、地下城、城市、自然景观、抽象艺术、迷宫、阈限空间等。

### 1.2 核心目标
- **保留资产**：最大化复用 MarkovJunior C# 引擎、CraftSharp-Resource、Unity ECS 渲染等现有代码
- **低门槛**：让非程序员通过可视化界面创建复杂的程序化结构
- **全版本**：支持任意 Minecraft Java 版本的资源包加载与方块映射
- **多入口**：支持图片、NBT/结构文件、自然语言、手动编辑四种输入方式
- **多出口**：一次设计，导出为 mcfunction、结构文件、数据包、通用 3D 模型等多种格式
- **无限感**：支持种子驱动的随机变体与无限延伸的世界生成
- **智能化**：集成 AI 辅助设计、图片风格参考提取与结构文件逆向分析
- **高自由度**：不绑定特定风格或玩法，提供底层能力供用户自由组合

### 1.3 技术栈
- **引擎**：Unity 6000 LTS（或更新版本）
- **语言**：C#（.NET Standard 2.1）
- **渲染**：Unity ECS + Burst Compiler + GPU Instancing
- **UI**：Unity UI Toolkit（UXML/USS）或 uGUI
- **资源解析**：CraftSharp-Resource（扩展版本适配层）
- **数据持久化**：SQLite（via Mono.Data.Sqlite 或第三方库）
- **AI 接入**：C# HttpClient 调用 LLM API，本地 Ollama 通过 HTTP 对接

---

## 二、系统架构

### 2.1 Unity 项目分层

```
┌─────────────────────────────────────────────────────────────┐
│                    Unity 前端层                               │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────────────┐  │
│  │ 规则节点编辑器 │ │  3D 预览窗口  │ │   导出与设置面板      │  │
│  │(UI Toolkit)  │ │(ECS + Burst) │ │   (mcfunction/RCON   │  │
│  └──────────────┘ └──────────────┘ │    /数据包/结构文件)  │  │
│  ┌──────────────┐ ┌──────────────┐ └──────────────────────┘  │
│  │ AI 交互面板   │ │ 图片导入向导  │ ┌──────────────────────┐  │
│  │(Chat UI)     │ │(Drag & Drop) │ │   资源包管理器        │  │
│  └──────────────┘ └──────────────┘ │   (版本选择/预览)     │  │
│  ┌──────────────┐ ┌──────────────┐ └──────────────────────┘  │
│  │ 结构文件导入  │ │ 逆向分析面板  │                             │
│  │(NBT/Schem/   │ │(模式提取/规则 │                             │
│  │ Litematic/Vox)│ │ 模板建议)     │                             │
│  └──────────────┘ └──────────────┘                             │
├─────────────────────────────────────────────────────────────┤
│                    C# 核心逻辑层 (Assembly)                   │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────────────┐  │
│  │ MarkovJunior │ │ 版本适配资源  │ │   数据包生成器        │  │
│  │ 规则引擎      │ │ 解析器        │ │   (Dimension/Biome)  │  │
│  │(直接复用源码) │ │(扩展CraftSharp)│ │                      │  │
│  └──────────────┘ └──────────────┘ └──────────────────────┘  │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────────────┐  │
│  │ 图像处理      │ │ AI HTTP 客户端│ │   规则序列化/反序列化 │  │
│  │(Unity Texture2D│ │(UnityWebRequest│ │   (JSON)             │  │
│  │ 或 ImageSharp) │ │ 或 HttpClient) │ │                      │  │
│  └──────────────┘ └──────────────┘ └──────────────────────┘  │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────────────┐  │
│  │ 结构文件解析  │ │ 结构分析引擎  │ │   规则模板生成器      │  │
│  │(NBT/Schem/   │ │(模式挖掘/对称 │ │   (从结构反推规则)    │  │
│  │ Litematic/Vox)│ │ 检测/分层)    │ │                      │  │
│  └──────────────┘ └──────────────┘ └──────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│                    Unity ECS 渲染层                           │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────────────┐  │
│  │ Voxel Render │ │ Chunk Manager │ │   Material/Texture   │  │
│  │ System       │ │ (加载/卸载)   │ │   Atlas Builder      │  │
│  │(InstancedMesh)│ │              │ │   (基于CraftSharp)   │  │
│  └──────────────┘ └──────────────┘ └──────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              ↓
                    ┌─────────────────┐
                    │  外部系统/文件    │
                    │  (MC服务器/文件系统)│
                    └─────────────────┘
```

### 2.2 跨平台策略
由 Unity 引擎原生支持 Windows、macOS、Linux 三平台构建。利用 Unity 的自动图形 API 选择（Windows 用 DirectX 11/12、macOS 用 Metal、Linux 用 Vulkan），无需手动处理渲染后端差异。

---

## 三、核心规则引擎

### 3.1 节点类型
引擎支持 MarkovJunior 的全部核心节点，并预留扩展接口：

- **One 节点**：每轮迭代随机选择并应用一条匹配规则
- **All 节点**：每轮并行应用所有匹配规则
- **Parallel 节点**：多规则并发执行，支持不同调度策略
- **WFC 节点**：波函数坍缩约束求解，用于受控的图案填充
- **Map 节点**：后处理映射，将符号批量替换为其他符号
- **Sequence 节点**：顺序执行子节点链
- **Markov 节点**：回退执行，失败时自动撤销并尝试其他分支
- **Group 节点**：逻辑分组，支持折叠与复用

### 3.2 内部表示
规则采用 JSON 作为内部序列化格式，替代原版的 XML。包含元数据、调色板定义、节点图与连接关系。调色板支持字符串键与完整方块状态（含方向、半砖位置等属性），突破单字母限制。

### 3.3 执行与调试
- **连续执行**：全速运行至完成或达到步数上限
- **单步执行**：每次前进指定步数，便于观察生长过程
- **断点调试**：支持在指定迭代次数自动暂停
- **规则命中统计**：记录每条规则的触发次数与覆盖率，辅助优化
- **热力图**：在 3D 预览中高亮显示网格中变更最活跃的区域
- **Undo/Redo**：对规则图的编辑操作与网格状态的生成过程均支持撤销与重做

### 3.4 性能优化
- **增量扫描**：模式匹配仅追踪受前一轮变更影响的邻域，避免全网格扫描
- **并行迭代**：大规模网格的规则匹配支持多线程并行（Burst Job System）
- **内存优化**：使用 NativeArray / NativeHashMap 等 ECS 原生容器，避免 GC Alloc

---

## 四、Minecraft 资源与渲染

### 4.1 全版本资源包支持
- **自动检测**：读取资源包 pack.mcmeta 中的 pack_format 字段，识别版本年代
- **版本适配层**：在 CraftSharp-Resource 之上封装 VersionedResourceLoader，根据 pack_format 自动切换解析策略，处理纹理路径、方块 ID 体系、Blockstates 格式等差异
- **客户端 Jar 直读**：支持直接加载 .minecraft/versions/ 下的任意版本客户端 jar，无需手动解压
- **第三方资源包**：支持加载 resourcepacks 目录下的用户资源包（ZIP 格式）
- **本地缓存**：解析后的方块模型、纹理图集与状态定义持久化到本地 SQLite，二次加载秒开
- **版本映射**：内置 Legacy 数字方块 ID 到 Modern 命名空间 ID 的完整映射表，兼容 1.12 及以前版本

### 4.2 3D 实时预览（Unity ECS）
- **体素渲染**：基于 Unity ECS + Burst Compiler + Graphics.DrawMeshInstanced 批量渲染，支持百万级方块实时更新
- **表面剔除**：仅渲染与空气相邻的表面方块，减少 60% 以上绘制量
- **Greedy Meshing**：将相邻同材质方块合并为更大矩形，进一步降低 Draw Call
- **Chunk 分块**：大场景按 16³ 或 32³ 分块管理，视口外 Chunk 自动卸载
- **LOD 系统**：远距离 Chunk 降级为简化表示或体素云
- **材质支持**：完整支持不透明、Cutout（Alpha 测试）与半透明三种渲染模式，通过 CraftSharp-Resource 加载真实方块纹理与 JSON 模型
- **相机控制**：Orbit 旋转、平移、缩放，支持正交与透视两种投影模式
- **实时生长动画**：规则引擎每执行一步，通过 ECS System 即时同步变更到渲染线程，可调整播放速度

---

## 五、可视化规则编辑

### 5.1 三种编辑模式
- **向导模式**：面向新手。通过回答 3-5 个问题（建筑类型、尺寸、材质、氛围），由系统或 AI 自动生成完整规则图
- **模块模式**：面向进阶用户。从预设模块库拖拽组件（地形生成器、建筑外壳、腐蚀模块、植被点缀等），通过连线组合并调整参数
- **专家模式**：面向高级用户。直接编辑 JSON 规则、编写原始规则字符串、自定义节点逻辑

### 5.2 节点编辑器
- **节点图界面**：规则以可视化节点图呈现，支持拖拽、连线、分组与自动布局
- **节点类型**：每种节点类型拥有独特的视觉样式与参数面板
- **连接验证**：实时检查节点间的输入输出兼容性，无效连接自动标红
- **参数面板**：双击节点展开详细编辑，包括规则列表、概率滑块、步数、种子等
- **模板库**：常用规则片段保存为可复用模板，支持导入导出
- **热重载**：修改规则后，后台自动在小网格上快速预演，3 秒内给出预览缩略图

### 5.3 预设模块库
内置可参数化的模块化规则集，用户可自由组合创造任意风格：
- 地形生成器（Perlin Noise 高度图、生物群系适配、洞穴系统）
- 建筑外壳（空心立方体、圆柱、球体、金字塔、拱顶）
- 随机腐蚀（边缘方块替换为空气或其他材质、风化效果）
- 植被点缀（在指定表面随机放置树木、花草、藤蔓）
- 道路连接（基于 WFC 或迷宫算法在两点间生成路径、桥梁）
- 室内填充（建筑内部生成房间布局与走廊网络、家具布置）
- 水体/岩浆生成（河流、湖泊、瀑布、地下熔岩河）
- 光照布置（火把、灯笼、萤石、红石灯的自动布置逻辑）
- 装饰细节（栏杆、楼梯、屋檐、立柱、壁画）

---

## 六、多模态智能生成

### 6.1 AI 自然语言生成
采用三层架构，避免 LLM 直接编写易错的原始规则：

- **意图解析层**：LLM 将用户自然语言描述解析为结构化意图（建筑类型、组件列表、氛围、复杂度）
- **模板编排层**：确定性 C# 代码根据意图从预验证模板库中选择并组合模块，自动填充参数
- **规则编译层**：将模板实例化为具体 JSON 规则，自动分配调色板、步数与种子默认值

支持多种 LLM 后端：OpenAI、Claude、本地 Ollama（Llama/Qwen 等）。用户自行配置 API Key，本地模型优先以保护隐私。

### 6.2 图片风格参考
提供两种工作流：

- **快速参考模式**（默认）：用户上传图片后，系统在 Unity 中提取主色调调色板（颜色量化），映射为最接近的 Minecraft 方块，生成初始二维方块层作为编辑起点。用户在此基础上手动编写规则或使用模板生长
- **Markov Senior 遗传算法模式**（高级）：用户上传图片并指定参数后，系统在后台运行遗传算法。以规则集为基因、生成结果与目标图片的差异为适应度，通过选择、交叉与变异迭代优化。支持分治法将大图切分为子区域独立进化后拼接。训练完成后输出优化规则集

### 6.3 结构文件逆向分析
支持从现有体素结构反推 Markov 规则模板，作为规则设计的起点。

#### 6.3.1 支持的输入格式
| 格式 | 来源 | 优势 |
|------|------|------|
| .nbt | Minecraft 结构方块 | 原生格式，含方块状态，最准确 |
| .schem / .schematic | WorldEdit、MCEdit | 建筑社区最常用，大量现成建筑 |
| .litematic | Litematica Mod | 含材料清单，建筑精度高 |
| .vox | MagicaVoxel | 独立体素艺术，风格独特 |
| JSON 网格 | 自定义导出 | 最灵活，可直接描述体素数组 |

#### 6.3.2 解析阶段（无损）
读取结构文件 → 解析为 3D 网格 [x][y][z] = block_id + block_state。用户可选择只分析子区域。此过程零信息损失，不存在图片颜色量化误差。

#### 6.3.3 结构分析阶段
对网格进行多层分析：

| 分析维度 | 输出 | 用途 |
|---------|------|------|
| 材质统计 | 各方块出现频率、颜色分布 | 自动生成调色板 |
| 层切片分析 | 每层的二维图案 | 识别重复楼层、地基模式 |
| 对称性检测 | 镜像轴、旋转对称阶数 | 生成对称规则 |
| 连通性分析 | 房间分隔、走廊网络、门窗位置 | 识别空间布局逻辑 |
| 局部模式挖掘 | 频繁出现的 2×2×2 / 3×3×3 模式 | 提取为 Markov 替换规则候选 |
| 结构分层 | 地基→墙体→屋顶→装饰的垂直分层 | 生成 Sequence 节点链 |

#### 6.3.4 规则模板生成（辅助而非自动）
根据分析结果，系统建议一套规则框架，用户在此基础上调整：

- 检测到重复楼层 → 建议 All 节点循环生长
- 检测到外墙竖条模式 → 建议 One/All 节点扩展规则
- 检测到金字塔逐层收缩 → 建议边缘空气侵蚀规则
- 检测到固定间隔窗户 → 建议定点替换规则

系统生成的是起点模板，不是正确答案。用户需要：调整规则概率、添加随机性、扩展变体。

#### 6.3.5 应用场景
- **建筑复刻+随机化**：导入 .litematic 城堡 → 分析结构 → 加入概率腐蚀与材质替换 → 导出 mcfunction 多变体
- **学习官方结构**：导入村庄房屋 NBT → 理解官方 Jigsaw 建造逻辑 → 创建自己的变体规则
- **体素艺术转换**：导入 MagicaVoxel 角色 → 映射为 MC 方块 → 生成"从脚向上生长"的规则
- **地形学习**：导入自然地形结构 → 提取坡度与材质分布模式 → 生成类似地形的规则

### 6.4 AI 交互面板
- 对话式界面，支持多轮迭代修正
- 生成结果附带解释文本，说明每条规则的作用
- 语法与语义自动验证，错误信息可反馈给 AI 重新生成
- 提供"自动修复"按钮，将验证错误回传 LLM 修正

---

## 七、导出系统

### 7.1 mcfunction 多变体导出
- **种子驱动**：同一结构根据 N 个不同种子预生成 N 个变体，打包为函数集合
- **主调度器**：自动生成入口函数，根据世界种子选择对应变体，确保同一世界始终生成相同结构
- **Fill 合并优化**：自动将逐块放置合并为 fill 命令，大幅减少命令数量
- **空气剔除**：自动去除无意义的空气放置命令
- **分片处理**：单文件超过 65536 条命令上限时自动拆分为多个文件并生成调用链
- **坐标原点**：支持以玩家位置或指定绝对坐标为原点

### 7.2 RCON 实时注入
- **网络连接**：通过 RCON 协议连接 Minecraft 服务器，支持密码认证
- **批量发送**：将变更命令按批次发送，控制速率避免服务器过载
- **Diff 机制**：仅发送每轮迭代中发生变更的方块，而非全网格重传
- **Fill 优化**：发送前对变更集进行实时矩形合并，优先使用 fill 而非 setblock
- **断线恢复**：持久化未发送队列，连接中断后可从中断点续传
- **动画效果**：支持调整发送间隔，在客户端形成"结构生长"的视觉效果
- **状态监控**：实时显示已发送数量、剩余数量与网络延迟

### 7.3 结构文件导出
- **.nbt 格式**：Minecraft 结构方块原生格式，支持旋转、镜像与结构方块直接加载
- **.litematic 格式**：Litematica Mod 标准格式，支持材料清单、投影粘贴与 Printer 自动放置
- **.schem 格式**：WorldEdit 兼容格式

### 7.4 通用 3D 格式
- **.vox**：MagicaVoxel 原生格式，带 256 色调色板
- **.obj / .glb**：通用三角网格格式，支持材质与纹理导出

### 7.5 数据包导出（自定义世界生成）
支持导出完整的 Minecraft 数据包，用于生成自定义维度与程序化世界：
- **维度定义**：自定义环境参数（光照、雾、天空、时间锁定、床/重生锚行为）
- **维度类型**：配置无限边界、逻辑高度、坐标缩放等
- **生物群系定义**：自定义地表规则、特征、结构与氛围
- **Jigsaw 结构系统**：
  - 将 Markov 生成的结构单元导出为 .nbt 结构模板
  - 自动在模板边缘放置 Jigsaw 接口，定义连接点类型与匹配规则
  - 生成 Template Pool，按权重随机选择变体
  - 生成 Structure Set，控制生成频率、间距与分布算法
  - 生成 Processor List，实现随机退化与变体（墙面破损、材质替换、空气腐蚀、植被覆盖）
- **应用场景示例**：
  - 无限城市：密集建筑单元通过 Jigsaw 拼接生成无限都市
  - 地下城网络：房间与走廊单元随机连接形成地下迷宫
  - 自然景观：地形单元与植被单元组合生成独特生态
  - 阈限空间：单调单元高频拼接配合压抑环境参数

---

## 八、示例应用场景

本工具不绑定任何特定玩法或风格，以下为部分示例场景，展示其自由度：

### 8.1 程序化建筑
- 随机城堡、村庄、神庙、塔楼
- 现代城市街区与摩天大楼
- 桥梁、港口、城墙等基础设施
- 室内空间：房间布局、走廊网络、家具布置

### 8.2 自然景观
- 山脉、峡谷、洞穴系统
- 河流、湖泊、瀑布水系
- 森林、丛林、沙漠地貌
- 火山、冰川、珊瑚礁等特殊地形

### 8.3 地下与迷宫
- 地下城、矿洞、遗迹
- 迷宫、密室逃脱地图
- 地铁隧道、下水道网络

### 8.4 抽象与艺术
- 分形结构、几何雕塑
- 像素艺术、体素雕像
- 纪念碑、花园、景观设计

### 8.5 特殊氛围空间
- 后室/阈限空间：无限走廊、单调重复、压抑照明
- 科幻场景：空间站、外星地表、赛博朋克街区
- 恐怖场景：废弃建筑、墓地、诡异森林

---

## 九、社区与市场

### 9.1 规则包格式
定义 .mjr 规则包标准（ZIP 压缩），包含：
- 元数据清单（名称、作者、版本、标签、描述、缩略图）
- 规则定义（JSON 格式）
- 调色板与方块映射表
- 预览图与使用说明
- 依赖声明

### 9.2 本地库管理
- SQLite 数据库存储已下载与自创的规则包
- 支持标签系统、收藏、评分与搜索筛选
- 按复杂度、风格、用途等维度分类浏览
- 一键导入编辑器或导出为 .mcr 文件分享

### 9.3 社区市场（可选云端）
- 规则上传与下载（支持版本管理与更新检查）
- 热门排行榜与标签云
- 可选的自托管 REST API 服务器

---

## 十、性能与优化

### 10.1 渲染优化（Unity ECS）
- ECS Entity 批量渲染与 Burst Compiler 加速
- 表面剔除与 Greedy Meshing
- Chunk 分块加载与视锥剔除
- 远距离 LOD 降级

### 10.2 引擎优化
- 增量模式匹配（仅扫描变更邻域）
- Job System 多线程并行规则应用
- NativeArray / NativeHashMap 零 GC Alloc

### 10.3 导出优化
- mcfunction 的 Fill 合并与空气剔除
- RCON 的 Diff 发送与批量压缩
- 数据包资源的本地缓存与增量更新

---

## 十一、实现路线图

### Phase 1：核心 MVP
- Unity 项目骨架与跨平台构建配置
- MarkovJunior 规则引擎核心集成（One、All、Parallel 节点）
- 基础 3D 预览（ECS + Burst + InstancedMesh）
- JSON 规则格式与基础编辑界面
- 内置 10-20 个示例规则（涵盖建筑、地形、迷宫等多种类型）
- mcfunction 多变体导出（含 Fill 合并优化）

### Phase 2：多模态输入与智能
- AI 自然语言生成（模板组合式）
- 图片风格参考提取（快速模式）
- Markov Senior 遗传算法模式
- 结构文件逆向分析（NBT/Schem/Litematic/Vox 导入 + 模式提取 + 规则模板生成）
- 预设模块库扩展（覆盖建筑、自然、地下城、抽象艺术等多种风格）
- RCON 实时注入功能
- .nbt 与 .litematic 结构导出

### Phase 3：生态与高级生成
- CraftSharp-Resource 版本适配层（全版本资源包支持）
- 数据包维度生成（Dimension + Biome + Jigsaw）
- 示例场景预设库（城市、自然、地下城、阈限空间等多种模板）
- 本地规则库与 .mcr 包管理
- 社区市场基础版

### Phase 4：打磨与扩展
- WFC 节点完整支持
- 更多导出格式（.obj、.glb、.vox）
- 本地 LLM 集成（Ollama HTTP 对接）
- 性能极致优化（大场景、高迭代）
- 跨平台发布与自动更新

---

## 十二、关键设计原则

1. **Unity 资产最大化复用**：保留 CraftSharp-Resource、ECS 渲染管线与现有 C# 逻辑，减少重写成本
2. **多模态输入**：图片、结构文件、自然语言、手动编辑四种方式并列，满足不同场景需求
3. **高自由度**：不绑定特定风格或玩法，提供底层能力供用户自由组合创造
4. **本地优先**：所有核心功能离线可用，AI 与云端为可选增强
5. **确定性**：同一规则 + 同一种子 = 同一结果，确保可复现性
6. **渐进式复杂度**：向导 → 模块 → 专家三级递进，不强迫用户学习底层语法
7. **格式无关的核心**：规则引擎与 Minecraft 解耦，支持导出到任意平台或游戏
8. **社区驱动**：规则包标准开放，鼓励分享与复用
